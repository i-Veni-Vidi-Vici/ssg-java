package com.sh._03_has_a;

/**
 * <pre>
 *     Has-a포함관계
 *     - 한 클래스에서 인스턴스 변수로 다른 클래스 타입의 참조변수를 가지고 있는 경우
 *     - UML(통합 모델링언어) 클래스 다이어그램 상에서는 연관(association)관계
 *     구분
 *     - 단방향/양방향 관계
 *      -단방향 Person ->Phone
 *      -양방향 Home <-> Person
 *     - 집약/합성 관계
 *     -
 *
 *
 * </pre>
 */
public class App {
    public static void main(String[] args) {

    }
}
