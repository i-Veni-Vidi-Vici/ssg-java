package com.sh.polymorphism_02._abstract;

/**
 * 추상 클래스 | 인터페이스
 * - 상속을 통해 클래스 계충 구조를 만들면서, 추상 메소드를 통해 구현을 강제화 할 수 있다.
 * - 추상 메소드란 ? 미완성 메소드로 몸통이 없다. 자식 클래스에서 반드시 구현해야 한다.
 * - 둘다 객체화 할 수 없다. (new 연산자 사용불가)
 *
 * 추상 클래스
 * - abstract class 미완성 클래스
 * - 추상 메소드를 통해서 자식 클래스으 ㅣ기능을 강제 구현하게 한다.
 * - 일반 필드 / 메소드를 가질 수 있다. (인터페이스와 다른점) 인터페이스가 메소드를 물려주려면 default메소드를 이용해야 한다.
 * - 0개 이상의 추상 메소드를 가지고 있다. (추상 메소드가 하나라도 있다면, 반드시 추상 클래스여야한다.)
 *
 */
public class App {
}
