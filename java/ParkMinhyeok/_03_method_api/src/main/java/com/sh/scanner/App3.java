package com.sh.scanner;

/**
 * <pre>
 * next 메소드 다음에 nextLine 메소드를 사용하는 경우
 * - 입력 버퍼에 남아있는 개행문자를 다음 nextLine 메소드가 읽어서 처리. 실제 입력값을 읽지 못함.
 * - 개행문자 제거용 nextLine 메소드를 미리 호출해야 한다.
 * </pre>
 */
public class App3 {
    public static void main(String[] args) {
        // 1. next 메소드 사용

        // 2. nextLine 메소드 사용
    }
}
